# Sync artifacts — generated by scripts/sync_from_litellm.py, only
# meaningful inside the CI workflow that opens the PR.
sync-diff.json
models.json.candidate
models.json.tmp

# Python venv if anyone runs scripts locally
__pycache__/
*.pyc
.venv/
venv/

# OS / editor junk
.DS_Store
.vscode/
.idea/
